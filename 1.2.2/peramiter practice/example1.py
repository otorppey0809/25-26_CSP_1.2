def add_numbers(one,two):
    sum = one + two
    return sum

print(add_numbers(20,21))

def multiply_numbers(one, two, three):
    product = (one + two) * three
    return product

print(multiply_numbers(20,21,22))